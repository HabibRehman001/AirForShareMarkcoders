# ZeroTierVpn

Optional / legacy ZeroTier experiments lived here.

The full **ZeroTierOne** source tree is **gitignored** (very large).  
For production VPN we use **WireGuard** (`../wireguard/`).

Install ZeroTier from: https://www.zerotier.com/download/
